def main () # proba txar 3

 
let _a_b, b, c : int; #! ERROREA: ez da _ hasieran onartzen. Hau analisi sintaktikoak emango du
 d___f, e : float;      #! ERROREA: ez dira hitu _ jarraian onartzen. d _ _ _ f hartuko ditu. _ EZ DA LEXIKOAN SARTU BEHAR TOKEN GISA

  def prozesatu(x: int; y: int)  # bi parametroak balioz
    let lag, bueltak_, $emaitza, emaitza__1: int;
    	f : float in
    '''' azpiprograma habiaratuak
     ''  sinbolo taula erabiliz gero, erabilera esparruak ondo kontrolatu '''
  {
    def gehibat (x: & int)  {
      x = x + 1;
    } # end
    def kenbat (x: & int) {
      x = x - 1;
    } # end
 
    lag = y; $emaitza = x;
    forever {
      break if emaitza__1 < 0 ;
      emaitza__1 = emaitza__1 - 1 * bueltak_ // y;
    } # begizta infinitua 

    if $emaitza < 1000 {
      bueltak_ = 0;
      do {
	# gehibat($emaitza); ''' prozedura ?? deiak tratatzeko '''
	if $emaitza > 100000 { next; }
	# kenbat(lag);	   ''' prozedura ** deiak tratatzeko '''
	bueltak_ = bueltak_ + 1;
      }  until lag != 0 			# ERROREA: /= egon behar du
      else { print($emaitza); }  #! do-until
   
      if emaitza__1 == 0000 { print(emaitza__1); } # ERROREA: ezin dira zeroak ezkerretara jarri osoetan
      if emaitza__1 > 0 { print(bueltak_); }
    } #! if amaiera
  
    print(bueltak_);
  } #! prozesatu amaiera

  def gehibi (x: & int)  { # parametroa erreferentziaz
    x = x + 2;
  } #! amaiera

'''*** hemen hasten da  ** / *
       programa nagusia ***''''

  read(a); read(b); read(f);
  e = 02.6743E2; # ERROREA: ezin dira zeroak ezkerretara jarri errealetan
  print(e);
  e = e * 4.900e+02; # ERROREA: ezin dira zeroak ezkerretara jarri errealetan esponentean
  print(e);
  # gehibi(f);
  e = e - 0.5;
  d = 1/e;
  println(d);
  # prozesatu(a,b);  ''''' Ondoren dauden ikurrak EZ DIRA TOKEN BERRIAK, iruzkin barruan daude * ** &%$·""?¿ñ^[]Çç:;-_,.\ | ! @   **** *** '''''
  c = c*(c*d)+e;
  print(c); print(f);
  
  # ERROREA: hainbat ikur oker,EZ DIRA TOKEN BERRIAK eta ez dira onartu behar!! Ez sartu lexikoan!!
  &%$·""?¿ñ^[]Çç:;-_,.\ |! # @ 

} # procedure main amaiera


